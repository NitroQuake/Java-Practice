Arrays store a fixed-size collection of elements of the same data type.
Make sure to import the util.

Arrays:

int[] numbers = { 2, 3, 5, 1, 4};
    //"[]" creates an array//
System.out.println(numbers.length);
    //tells how many are in that array//
Arrays.sort(numbers);
    //"sort("name of the method")" sorts the array//
System.out.println(Arrays.toString(numbers));
    //tells what's in the array//

Multi-dimensional Arrays:

int [][] numbers = { {1, 2, 3}, {4, 5, 6}};
        //"[][]" creates more colums or arrays//
System.out.println(Arrays.deepToString(numbers));
    